# 项目决策记录

> 本文档记录项目所有正式决策。决策一旦写入即锁定，修改需经会议讨论。
> 维护者：PM

---

## D001 - 技术选型

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 采用 Python + Tkinter 开发桌面客户端 |
| **理由** | Python 环境已具备；Tkinter 原生桌面、轻量、可打包为 exe；符合"非网页游戏"需求；开发复杂度与项目体量匹配 |
| **备选方案** | Node.js + Electron（体积过大，被否决） |

---

## D002 - 分支模式

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 采用模式 B：带状态变量的分支系统 |
| **理由** | 用少量节点组合出丰富变化，支持直接分流、阈值判定、组合判定三种节点类型；维护成本低于纯树形分支 |
| **节点类型** | ① 直接分流（选A去A页，选B去B页） ② 阈值判定（变量 >= N 时解锁某选项） ③ 组合判定（多变量同时满足条件时触发） |

---

## D003 - 剧情数据存放

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 剧情数据存放在独立 JSON 文件中（`data/` 目录），代码与剧情分离 |
| **理由** | 改剧情不需动代码，降低维护门槛；JSON 为纯文本，任何编辑器可打开 |

---

## D004 - 存档系统

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 一期包含 3 个手动存档位 + 1 个自动存档位 |
| **理由** | 文字冒险游戏必须支持存档；实现难度低；关键选择前自动存档降低玩家挫败感 |

---

## D005 - 界面风格

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 基础色调：深灰底（`#1a1a2e`）+ 淡白字（`#c8c8c8`）；关键文本：血红（`#cc0000`）；整体营造心理惊悚氛围 |
| **理由** | 吻合灯塔内部昏暗环境；为红色抖动文字提供视觉对比基础 |

---

## D006 - 红色抖动文字功能

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 加入。关键恐怖描写时触发红色抖动文字效果 |
| **使用规则** | 每章节出现 2-3 次，不滥用；配合 Tkinter `Text` 组件的 tag + 定时器实现 |

---

## D007 - 多次点击按钮功能

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 加入。特定剧情选项需连点 3-5 次才能生效 |
| **表现方式** | 每次点击时按钮文字变化（展示内心独白）；按钮颜色逐渐变红 |
| **使用范围** | 1-2 个关键剧情点 |

---

## D008 - 核心故事引擎

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | "两个普通人在与世隔绝的灯塔上，逐渐分不清超自然现象与自我崩溃的边界" |
| **核心原则** | 模糊现实与疯狂的界限；多用白描；让玩家自己脑补恐惧 |

---

## D009 - 状态变量

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **显性变量** | 好奇心 (Curiosity, 0~10)、理智 (Sanity, 0~10)、信任 (Trust, 0~10) |
| **隐藏变量** | 生存意愿 (Survival Will, 0~10)、忠诚 (Loyalty, 0~5) |
| **变量联动** | 各选择同时影响 1-3 个变量；关键节点通过阈值/组合判定分流 |

---

## D010 - 小游戏

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **配电设备连线（第2天）** | 点击匹配接线端子，全部正确配对完成 |
| **太阳能板反应测试（第2天）** | 点击或空格键响应闪烁光点，漏掉影响效率评价 |
| **直升机平台平衡测试（第4天）** | 持续点击保持光标稳定在中心区域 |
| **小游戏原则** | 调味剂，不喧宾夺主；纯鼠标操作为主，键盘可选 |

---

## D011 - 结局数量

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 5 个结局（A~D 常规结局 + E 隐藏结局），由状态变量组合判定 |

---

## D012 - 文字体量

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 总文本量上限 12 万字，预估 6 章 + 回忆闪回 |

---

## D013 - 注释规范

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 全部代码使用中文注释；每个 `.py` 文件开头有模块说明；每个函数/类有功能说明；关键逻辑行有行内注释 |

---

## D014 - 记忆文档系统

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 项目使用 5 份记忆文档：`decisions.md`、`design.md`、`progress.md`、`changelog.md`、`test_report.md` |
| **目的** | 供开发团队与老板随时查阅项目状态、决策依据和修改记录 |

---

## D015 - 小游戏叙事化：黑暗收缩死亡场景

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 小游戏不仅用于调剂，更承载叙事和决定游戏走向的功能。首个应用场景：停电 + 阴影吞噬光源 + 玩家跑向 4 楼重启供电。 |
| **具体规格** | 界面边缘黑暗矩形向内收缩（50秒倒计时），小游戏居中不受覆盖；失败 → 角色死亡，触发特殊死亡结局 |
| **死亡呈现方式** | 同一界面分两阶段：①收缩动画 + 小游戏；②黑幕全覆盖 → 留白 1-2 秒 → 血红/白色文字渐显，死前独白与画面描述合为一段（3-5 句） |
| **死亡结局归类** | 特殊死亡结局单列为"灯塔未解死亡报告"，与 5 条主线结局平行 |
| **技术实现** | 封装 `DarknessOverlay` 类；V1 矩形收缩 → 效果不满意可升级 V2/V3/V4 |
| **文字浮现方式** | 逐字打印，速度略慢于正常阅读 |

---

## D016 - 黑暗收缩效果技术路线

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **V1（基线）** | 单一矩形边框向内收缩，规整流畅 |
| **V2** | 每边独立收缩速度，产生不对称挤压感 |
| **V3** | 多块小矩形随机分布在边缘，模拟"啃咬吞噬" |
| **V4** | 预渲染 PNG 遮罩动画帧逐帧替换，最精致 |
| **架构原则** | `DarknessOverlay` 类独立封装，内部实现可随时替换，不影响游戏逻辑 |

---

## D017 - 角色命名

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 队友正式命名"张海生"（48岁），男主/玩家角色命名"江临"（27岁） |

---

## D018 - 角色状态面板

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 采用弹出式浮层面板，Tab 键呼出/关闭；笔记风格，用隐晦文字代替数字；含"今日笔记"摘要；面板弹出时暂停文本推进 |

---

## D019 - 物品栏

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 同面板第二页，纯文字列表 + 鼠标悬停简述；只读不可操作；数量 ≤10 件；物品作用由剧情节点条件判定自动触发 |

---

## D020 - 日记残页道具

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 1930年代前任守塔人日记残页，4段文本分别在游戏不同节点揭示；位置在底层储藏室防水铁盒；发现条件 curiosity ≥ 3；关键页被张海生撕去藏于床头夹层 |

---

## D021 - 按钮交互创新（待细化）

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **移动按钮** | 每次点击后按钮变换位置，N 次后成功点击，GD 后续提交完整方案 |
| **隐藏按钮** | 按钮藏在文字区域内，玩家移动鼠标寻找，悬停时显现，GD 后续提交完整方案 |
| **状态** | 两个设计待方案完善后由老板拍板，再正式入库 |

---

## D022 - 角色背景与双人弧线

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 张海生 + 江临完整角色背景（约 11000 字）已通过；14天双人关系弧线 + 双视角表 + 结局状态表已通过；父子投射用行为/闪回/视角暗示，不在文本中点明 |

---

## D023 - 阶段1收尾，阶段2+3启动

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 阶段1前期设计全部完成（100%）；阶段2程序骨架搭建（FD）+ 阶段3剧情编写（ND）并行启动；FD 搭建引擎，ND 编写第1章 JSON + 日记残页原文 |

---

## D024 - 配色方案修正

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **修正** | 底色从 `#1a1a2e` 修正为 `#000000` 纯黑，与 Undertale 风格对齐 |
| **理由** | 纯黑底色下文字对比更强，更符合心理惊悚氛围，实际实现已全部采用纯黑 |

---

## D025 - 全量重建游戏客户端

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 删除全部旧版 src/*.py 源文件，按 design.md 完全重建。保留 data/chapter_01.json 文本底稿，保留全部 docs/*.md 文档不动 |
| **理由** | 旧代码经 4 轮打补丁后模块内部状态不一致、布尔标志交叉污染、3 个模块未实际接入、方法被绕过调用。全删消除隐性残留和冲突 |

---

## D026 - 重建顺序：按界面流

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 重建不按模块，按玩家实际体验的界面流顺序推进。每个迭代结束产出可运行的增量 |
| **迭代顺序** | ① 最小窗口验证 ② Game 状态机骨架（TITLE→TYPING→WAITING→CHOICES→MINIGAME→SAVE_LOAD）③ 标题界面+菜单 ④ 第1章逐字打印+分块翻页 ⑤ 选项+变量+节点跳转 ⑥ 存档/读档 ⑦ 状态面板 |
| **原则** | 每个迭代 QA 验证通过后老板可运行查看 |

---

## D027 - 重建第一步：最小窗口验证

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-08 |
| **决定** | 第一步只建一个纯黑 Tkinter 窗口，显示"灯塔"二字，验证字体（微软雅黑粗体 13px）、配色（#000000/#e8e8e8）、窗口尺寸（860×640）在目标机器渲染正常 |
| **工作量** | ~30 行代码 |

---

## D028 - 布局修正：菜单固定在窗口底部

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-09 |
| **决定** | choice_frame 改为 side=BOTTOM（status_bar 上方），文本区 expand 填空余空间。菜单始终在视野内 |
| **理由** | 旧布局 text_frame expand 贪婪占用全部空间，把菜单挤出可视区底部外 |

---

## D029 - SD 音效设计师加入 + 音效基础架构

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-10 |
| **决定** | 新增 SD（音效设计）角色。团队由 6 人扩充为 7 人编制（PM/GD/ND/FD/UI/QA/SD）。SD 负责全部音频体验设计，技术路线纯代码合成（wave + struct），零外部依赖/零素材文件。SD 不修改游戏逻辑代码，通过 sound_manager.py API 提供接口 |
| **职责** | 氛围音 + 事件音 + 静默设计。每章 ≤3 种声音事件；低频 + 留白 > 频繁打断；音效服务于叙事焦虑 |
| **M4 交付** | `src/sound_manager.py` 空壳 API（play/stop/stop_all/set_volume/mute/unmute）；`docs/sound_spec.md` 规范文档（含静默规则表）；`data/sounds/` 目录占位；QA 基础验收（SD-01~04） |
| **M5 交付** | 5 核心音效实现（hum_low/heartbeat/metal_creak/water_drone/silence_fade）；`[sound]` 标记引擎解析；各章节音效触发点标注 |
| **M6 交付** | 音效时序调优 + 音量控制 + exe 打包验证 |
| **技术决策** | ① 预生成策略——所有音效在 SoundManager.__init__ 时一次性合成到 data/sounds/ 缓存，避免实时合成 I/O 延迟 ② 异步播放——不阻塞 Tkinter 主线程，保障逐字动画流畅 ③ 存档预留——save 字典增加 sound_state 字段（M4 空对象占位）④ 静默规则——章节过渡强制静默 0.5s、面板打开淡出到 0、选项出现音量降至 20% ⑤ 环境音效自动化——夜间节点根据 chapter/day 自动触发，ND 只标记事件音效 ⑥ `[sound]` 生命周期与 `\n\n` 文本分块绑定——块内触发、块外停止 |

---

## D030 - PM 文档同步纪律

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-10 |
| **决定** | PM 每次讨论/会议结束后，必须将结论同步至对应文档：① 项目级决策 → `decisions.md` ② 角色职责/规格/设计 → `design.md` ③ 优化需求 → `optimizations.md` ④ 里程碑/进度变更 → `progress.md`。漏同步 = 决策未生效，任何人有权拒绝执行 |
| **落入位置** | `work_standards.md` W001 第二条（PM 文档同步）

---

## D031 - 代理人设文档

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-10 |
| **决定** | 建立 `docs/agents.md`，7人角色索引表。新会话启动时 PM 只需 `调阅 docs/agents.md` 即可恢复全体角色身份，无需重新召集 |
| **格式** | 全局技术约束（5条）+ 角色表格（7行×5列：角色/职责/原则/对标/边界）+ 顶部状态行 |

---

## D032 - GM 调试栏

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-10 |
| **决定** | 新增 GM 调试栏，快捷键 `Ctrl+Shift+G`，右侧滑入面板（宽 35%）。功能：① 节点直跳（按章分组列表，点击即跳，不自动存档）② 变量调节（5 变量 +/- 按钮）③ 预设组合（4 组结局方向预设） |
| **启用方式** | 启动参数 `--gm`（`python src/main.py --gm`），不加参数时 GM 栏完全不加载，零运行时开销 |
| **不污染存档** | GM 跳转不触发自动存档（`cmd_gm_jump` 跳过 `_auto_save`），调试行为不影响玩家进度 |
| **原则** | 每角色 3-4 条优先级排序的原则（P1-P4），全部从现有 MD 文档中提取，不新增设计内容 |
| **维护** | PM 维护，角色原则变更时间步更新 | |

---

## D033 - Bug 复盘与工作流强化

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-11 |
| **决定** | FD/QA/GD/SD 四角色各新增 P4/P5 原则写入 `agents.md`。FD：交付前自检 + 回归三点 + 空壳禁令。QA：人手令 + 双面验证 + 启动即测。SD：空壳禁令。GD：标记注册表。 |
| **依据** | B002~B011 十条 bug 复盘，根因归类为五条：① 零自检 ② 空壳当完成 ③ 单点验证 ④ 逻辑替代交互 ⑤ 无回归意识 |
| **标记注册表** | `data/markup_registry.json` 留到第 4 章启动时一并创建 |

---

## D034 - GD 5 结局精确定量阈值表正式交付

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | GD 正式交付 8 结局精确定量阈值表。`design.md §2.5` 中的 8 结局判定条件（含优先级、阈值、AND/OR 逻辑）即为最终阈值表，与 `story_engine.py check_ending()` 逐项对齐验证通过。G（3 flags 全 True）→ death（MG4/MG5 失败）→ F（c≥7+s≤3+t≤3+l≤2）→ A（c≥7+s≤3+(t≥5 or l≥3)）→ E（sw≥8+s≤4+t≤4）→ B（c≥6+s≥6+t≥7+l≥4）→ D（c≤3+s≥6+t≤3+l≤2）→ C（c≤3+s≥6）。E 结局已从扩展槽升级为正式结局。 |
| **验证** | PM 核验：`design.md §2.5` 与 `story_engine.py:96-135` 全 8 结局条件逐字一致，无偏差。FD 无需额外修改代码。 |

---

## D035 - 角色交付不可绕过 + 声明确认制

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | 建立 W005（角色交付不可绕过规则），各角色交付时必须输出规定格式的声明，PM 在收到声明前方可更新进度。同时扩充 `agents.md` 会话规则 #2（声明确认制）和 #3（角色隔离）。 |
| **动机** | 单人执行模式中，角色间制衡（FD→QA→PM 放行）被跳过——FD 不自检、QA 不验收、PM 直接标完成。W005 用强制性格式声明建立不可绕过的卡口。 |
| **落入位置** | `work_standards.md` W005 + `agents.md` 会话规则 #2/#3 |

---

## D036 - 异步回调防卫制（B019 MG2 竞态复盘）

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | 建立 W006（异步回调防卫），所有 Tkinter `after()` 回调须遵循 cancel 旧→设状态→schedule 新。FD P5 扩充"异步防卫"。`fd_selfcheck.md` 交互矩阵追加 X7（计时器到期瞬间点击）。 |
| **依据** | B019 — MG2 光点叠层：`_hit_dot` 和 `_miss_dot` 各调度 `_clear_dot_and_next`，竞态下 `_spawn_flash` 重复调用导致光点成对出现并位置叠加。根因是 `_spawn_timer` 未被 cancel。 |
| **落入位置** | `work_standards.md` W006 + `agents.md` FD P5 + `fd_selfcheck.md` X7 |

---

## D037 - 桥设计澄清：两类作用 + 主线固定结构

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | ① 桥分两类：类型一承载分叉叙事（正文中与某条路径冲突的内容移入桥），类型二只写体验过渡（50-100 字简述"怎么过的"）。② 桥只回答"怎么到的"，不回答"路上发生了什么"；对话/揭示/情节转折属正文或类型一桥。③ 主线是固定线性叙事——选项改变变量、不产生剧情分叉；选择后的不同体验由 `text_bridges` 承载。 |
| **动机** | 桥设计定位偏差：之前被当作"微型叙事场景"写作，导致剧透（"十年前他也看到过"）、时序倒置（先评"回答很短"后见"热胀冷缩"）、重复叙事（桥节点 + text_bridge 写同一场景两次）。需明确桥的边界和类型区分。 |
| **落入位置** | `bridges.md` §1 桥的两类作用 + §2 原则 #10 + 黄金规则 / `design.md` §2.1 结构标注 / `agents.md` ND P4 桥分类引用 |

---

## D038 - 小游戏生命周期防卫（MG4A 崩溃复盘）

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | 建立 W007，小游戏完成回调不得在渲染循环调用栈内同步执行销毁。`_complete` 通过 `after(10)` 延迟回调；`destroy` 无条件执行 `_on_stop`；`_on_stop` 不得有条件 return。`fd_selfcheck.md` 加 X8（MG4A 连中 3 次→安全销毁）。 |
| **依据** | MG4A 崩溃链：`_check_hits → _complete → callback → destroy(canvas=None)` 在 `_loop` 栈内同步执行，`self.canvas.after(...)` 访问 None。87 测试未覆盖——因为 MG4A 无自动化测试、`_loop` 仅在真 Tkinter 主循环运行。W006 覆盖的"竞态"不覆盖"同步嵌套调用中 canvas 被销毁"——这是生命周期管理问题。 |
| **落入位置** | `work_standards.md` W007 + `agents.md` FD P5 + `fd_selfcheck.md` X8 |

---

## D039 - QA 角色重定义：测试审计员 + 两层分工

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | QA 从"独立放行官"重定义为"测试审计员"。去放行权，改出验证记录给老板做决策。两层分工：逻辑层 AI 覆盖（自动化+模拟点击+FD 声明二次确认+Bug 复现记录），物理层老板覆盖（视觉/动画/音效/排版）。Bug 关闭须老板物理验收。 |
| **动机** | 单人项目结构塌缩——FD/QA/PM 由同一 AI 承担，"独立验收"形同虚设。QA 的约束从"文档规定"改为"交付物对老板可见"——老板验收时检查 QA 验证记录。 |
| **落入位置** | `agents.md` QA 行 + `work_standards.md` W003 + `bug_list.md` 维护人 |

---

## D041 - 标记注册表维护流程 + Bug 设计级分级

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-14 |
| **决定** | ① 标记注册表从"一次性创建"改为"持续维护"——FD 实现/变更标记 → GD 同步更新 `data/markup_registry.json` status+engine 字段 → ND 查表后写入 JSON。`agents.md` GD P4 已更新。② Bug 分为两级：纯代码 Bug（不改 MD 设计值）→ FD 立即修复；设计级 Bug（改动 design.md §2.3/§2.5 阈值/条件/effects、state_manager 初始值、markup_registry 参数约束）→ FD 标"待分级"报 PM，老板决断后更新 MD 再修复。`agents.md` FD P2 已追加该逻辑。 |
| **动机** | 专项会议讨论 agents.md 角色冲突 #1（GD/FD/ND 循环依赖）和 #2（FD Bug 立即修复 vs PM 设计变更须老板确认）。前者根因是文档滞后——注册表实际已存在，GD P4 仍写"待创建"；后者缺 Bug 分级，可能让 FD 越过设计边界自行改阈值。 |
| **落入位置** | `agents.md` GD P4 行 + FD P2 行 |


## D040 - 分支对称性（结局循环复盘）

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-12 |
| **决定** | 建立 W009，同一状态判断逻辑必须在所有调用路径中对称实现。`fd_selfcheck.md` 加 X9（`is_ending_node` 路由）。 |
| **依据** | ch06 结局循环 — `is_ending_node` 转发仅在 `_on_text_done` 实现，`cmd_choice` 遗漏。同类：B018 / MG4A `_on_stop` / `_complete` 同步回调——均为一逻辑两路径不对称。 |
| **落入位置** | `work_standards.md` W009 + `fd_selfcheck.md` X9 |


## D042 - 角色边界澄清三案

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-14 |
| **决定** | ① UI/FD 边界"不新增交互模式"修正为"不**自行**新增（GD 提案+老板确认的除外）"。新交互模式走流程：GD 提案 → UI 兼容审查 → FD 技术评估 → PM/老板拍板 → 实现。② 音效暂停期间 ND 继续标注 `[sound]`，标准不降，避免恢复后返工。`markup_registry.json` 追记该指引。③ 跨角色同级 P 冲突建立三级升级链：L1 PM 据 MD 文档裁决 → L2 PM 主持 mini 讨论入 MD → L3 老板拉会终裁。替代原有的"灰色地带直接上会"。 |
| **动机** | PM 逐角色审查 agents.md 冲突隐患报告的 #3（交互模式归属灰色地带）、#4（SD 暂停连锁影响）、#5（同级 P 冲突无裁决顺序）。三案均属防患未然——当前无阻塞但随时触发。 |
| **落入位置** | `agents.md` UI/FD/ND 行 + 使用说明（升级链+新交互流程）+ `markup_registry.json` [sound] 条目 |


## D043 - 低优先隐患三案

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-14 |
| **决定** | ① QA P2 "未完待工"扩充代码层——要求 FD/SD 交付时附带"已实现 x 个方法，骨架占位 y 个"，QA 据此验证空壳禁令。② FD P5 回归三点明确触发文件范围——修改 `main_window.py`/`darkness_overlay.py`/`minigame_base.py` 触发三场景回归；仅改 `main.py`/`story_engine.py`/`state_manager.py`/`save_manager.py`/`sound_manager.py` 不触发。③ W005 声明遗忘建立修复通道——PM 放行前主动核查，遗漏则提醒补交，不阻断流程。 |
| **动机** | PM 冲突审查报告 #6（空壳无验证）、#7（声明遗忘=流程阻塞）、#8（回归三点范围模糊）。三案均为一两句话的边界澄清，防患未然。 |
| **落入位置** | `agents.md` QA P2 / FD P5 / 使用说明 |


## D044 - QA 留痕纪律强化

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-14 |
| **决定** | QA 从"被动记录员"升级为"主动留痕审计"——三处加强：① 每份 QA 声明须附实际命令输出（如 `87/87 PASS`），禁止模糊的"测试全绿"陈述。② 新增 FD 声明一致性检查——FD 声称验证了 X，QA 须确认对应测试脚本已跑过，不匹配标红。③ 数据完整性——每次 FD 代码改动后主动跑全量测试+validate_data，节点减少/引用断链立即标记。④ Bug 追溯补充——每次 FD 代码改动后主动扫描变更文件，比对 bug_list 是否有相关条目未更新。W005 QA 声明格式同步更新。 |
| **动机** | 老板指出 QA 退化为"记录员"——bug_list 只有老板提醒才更新，验证记录缺乏可追溯证据。D039 摘掉了 QA 的放行权但 QA 做的事仍然太薄。本次加强让 QA 的验证记录从"模糊的 PASS"变成"可被老板一眼看到的具象输出"，提高审计痕迹的防伪门槛。 |
| **落入位置** | `agents.md` QA P1/P2/P3/边界 + `work_standards.md` W005 QA 声明格式 |


## D045 - 小游戏全面优化方案

| 项 | 决定 |
|---|------|
| **日期** | 2026-05-14 |
| **决定** | 五批次小游戏优化（B1~B5），分批交付独立验证。B1 全局 UI：minigame_area 从全屏→居中 700×550 固定框架，外框 #555555 3px，所有小游戏自动受益。B2 MG1 配电：6对端子+6中继点+亮灭灯循环(亮1.5s/灭2.5s)；左→中继→右三段配对，灭灯可操作(靠记忆)，30s时限。B3 MG2 太阳能(待)：缩小+蓝干扰+移动+双点。B4 MG4A 海鸟(待)：25秒计时+红鸟+鼠标归正+鸟头骨flag。B5 MG4B/MG5 黑暗收缩(待)：暗红收缩+分屏配电组合+能量条过热。 |
| **动机** | 老板会议提出四小游戏难度太低/体验差，需全面优化。GD/FD/UI/PM 联合讨论确定方案。 |
| **落入位置** | `design.md` §2.4 + `progress.md` M6 + `minigame_base.py` / `darkness_overlay.py` / `main_window.py` |
